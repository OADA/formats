{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "lerna run build",
    "build": "bundle exec jekyll build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  },
  "name": "oada-formats-v2"
}
